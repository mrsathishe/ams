import styled from "styled-components";
import { StyledButton } from "../styled/StyledComponents";

export const ThemeLabel = styled.span`
  margin-left: 0.5rem;
`;

export const CompactThemeButton = styled(StyledButton)`
  width: 40px;
  height: 40px;
  padding: 0;
`;